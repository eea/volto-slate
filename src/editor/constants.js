// TODO: move this in settings.slate
import { settings } from '~/config';

// TODO: remove this duplicate code (somewhere else there is a function that returns an empty paragraph)
export const initialValue = [
  {
    type: 'paragraph',
    children: [{ text: '' }],
  },
];
